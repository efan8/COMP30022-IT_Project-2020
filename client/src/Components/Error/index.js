/* Basic Error page for testing non existent pages */

import React from 'react';

function Error() {
    return(
        <div>
            <p>Error No page exists</p>
        </div>
    );
};

export default Error;